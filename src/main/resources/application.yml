spring:
  application:
    name: T1
  datasource:
    url: jdbc:postgresql://localhost:5432/tasks
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
  mail:
    host: smtp.gmail.com
    port: 587
    username: t36960484@gmail.com
    password: *
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true